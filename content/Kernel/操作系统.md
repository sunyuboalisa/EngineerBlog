---
title: 操作系统
draft: false
tags:
---
# 逻辑模型
调用一切硬件资源的中心
发出各种指令，以使用硬件资源
## 硬件管理

## 系统调用

# 概念模型
- **OS 蓝图:**
    - **应用层 (apps):** `sh`, `echo`, 等。
    - **接口层:** 系统调用 (`open`, `read`, `fork`, ...)。
    - **内核层 (kernel):** 实现系统调用，管理硬件。
    - **硬件层:** CPU, 内存 (RAM), 磁盘。

# 计算机模型
bios

常见cpu架构以及指令集

| 架构类型 | CPU                                                        | 应用场景 | 指令集                                                                                                                                                                                                                               |
| ---- | ---------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| x86  | **Intel** Core Ultra、**AMD** Ryzen (Zen 4, Zen 5, 未来Zen 6) |      | 支持**AVX**系列（如AVX2, AVX-512）、未来将统一支持**AVX10**；支持**AVX-512**[](https://www.ithome.com/0/895/903.htm)[](http://ceea.org.cn/ceeacms/webArticleAction!detailView.do?articleID=202511173481607404623347686251)，Zen 6将增强**AVX512 FP16**等 |
| ARM  | **Apple** M系列、**高通** Snapdragon X系列                        |      | 原生**ARMv8**/**v9**指令集，集成NPU；原生**ARM**指令集                                                                                                                                                                                          |

| 特性       | x86 (及其64位扩展 x86-64/AMD64)                 | ARM                                                      |
| -------- | ------------------------------------------ | -------------------------------------------------------- |
| **设计哲学** | **CISC**，但内部使用微码转换为类RISC的微操作               | **RISC**，强调精简和能效                                         |
| **商业模式** | **垂直整合**：Intel和AMD自己设计、制造、销售芯片             | **IP授权**：ARM公司设计核心，授权给其他公司（苹果、高通、谷歌等）集成到自己的SoC中          |
| **主导领域** | **传统桌面和服务器市场**：Windows/macOS/Linux PC、数据中心 | **移动和嵌入式市场**：智能手机、平板电脑、物联网。**新兴领域**：苹果Mac、高通/联发科笔记本、云服务器 |
| **生态壁垒** | **软件生态**：拥有历史悠久的、海量的桌面应用和企业级软件             | **移动生态**：主导了Android和iOS应用生态                              |
重要的“其他”架构

尽管市场份额小，但这些架构在特定领域至关重要，并且可能影响未来。

- **RISC-V**
    
    - **最大特点**：**开源、免费**。其指令集规范可以自由使用，任何人都可以基于它设计CPU而无需支付高昂的授权费。
        
    - **定位**：被誉为“第三大主流架构”。从极低功耗的物联网MCU，到高性能的AI加速器，再到甚至超级计算机（欧洲处理器计划），都有它的身影。它代表着**定制化、模块化**的未来趋势。
        
    - **现状**：处于快速发展期，生态在快速构建中，是学术界和产业界的新宠。
        
- **Power (IBM)**
    
    - **领域**：曾经挑战x86的霸主，现在主要集中于**高性能计算、高端企业级服务器和大型主机**。例如，美国Summit超级计算机就使用了POWER架构。
        
    - **特点**：技术非常先进，但成本和生态限制了其普及。
        
- **MIPS**
    
    - **历史**：经典的RISC架构，曾广泛应用于网络路由器、嵌入式设备、游戏机（如PS1/PS2）。
        
    - **现状**：在市场竞争中逐渐被ARM和RISC-V取代，影响力已大不如前。

查看自己cpu架构
windows
```cmd
echo %PROCESSOR_ARCHITECTURE%
#更详细的可以用coreinfo工具
```
linux
```sh
lscpu
# 或者
cat /proc/cpuinfo
```
## 处理机
## 存储器
### 内存分配
#### 静态分配
#### 动态分配
#### 页面分配
页面寻址
内存地址结构
页面及页内偏移位置计算公式
## 文件
## IO



# xv6-risc-v
## 设计概览
- **OS 蓝图:**
    - **应用层 (apps):** `sh`, `echo`, 等。
    - **接口层:** 系统调用 (`open`, `read`, `fork`, ...)。
    - **内核层 (kernel):** 实现系统调用，管理硬件。
    - **硬件层:** CPU, 内存 (RAM), 磁盘。

## 进程
### 隔离
隔离是设计一个受保护内核的主要原因。想象一个没有操作系统的设计，应用程序直接与硬件交互。虽然高效、灵活，但主要问题是缺乏隔离：

- **资源隔离:** 一个应用可能耗尽内存、独占CPU或占满磁盘空间，影响其他应用。
- **内存隔离:** 一个应用的bug可能会意外写入另一个应用的内存空间，导致崩溃或数据损坏。

**UNIX 系统调用接口通过抽象硬件资源来帮助实现隔离：**

- **`fork()` 和进程** 抽象了CPU核心，允许OS在多个进程间透明地切换，实现分时复用。
- **`exec()`/`sbrk()` 和虚拟地址** 抽象了物理内存，使得每个进程都拥有自己独立的地址空间。
- **文件** 抽象了磁盘块，由OS负责布局和权限控制。
- **管道** 抽象了进程间的内存共享。
#### 硬件层面的隔离机制
CPU和内核是协同设计的，提供了实现隔离的基础：

1. **用户/监控模式 (User/Supervisor Mode):**
    
    - **监控模式:** 可以执行所有“特权”指令，如访问硬件、修改页表等。
    - **用户模式:** 不能执行特权指令。
    - 内核运行在监控模式，应用程序运行在用户模式。
2. **虚拟内存 (Virtual Memory):**
    
    - **页表 (Page Table):** 将虚拟地址（VA）映射到物理地址（PA），限制了进程可以访问的内存范围。
    - 每个进程都有自己的页表，由内核设置和管理，只能在监控模式下更改。
### 系统调用
应用程序需要一种受控的方式来请求内核服务。`ecall` 指令就是这个机制：

1. **切换到监控模式。**
2. **跳转到内核中一个预定义好的入口点。**

这确保了控制权安全地转移给内核，内核在处理完请求后，再将控制权交还给应用程序。我们将在后续课程中深入探讨这个过程。

### 内核设计哲学：单体 vs. 微内核

- **单体内核 (Monolithic Kernel):**
    
    - 内核是一个单一的、巨大的程序，实现了所有系统调用（xv6, Linux）。
    - **优点:** 子系统间易于协作，效率高。
    - **缺点:** 内部交互复杂，容易产生bug，一个驱动的bug可能导致整个系统崩溃。
- **微内核 (Microkernel):**
    
    - 内核只提供最基本的服务（IPC、内存管理、进程管理）。
    - 其他服务（文件系统、网络、驱动）作为普通的用户进程运行。
    - **优点:** 模块化，更健壮（服务可重启），潜在更安全（特权代码少）。
    - **缺点:** 性能可能是个问题，因为服务间通信需要通过IPC，开销较大。
